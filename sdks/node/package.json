{
  "name": "smolvm-embedded-node-workspace",
  "private": true,
  "scripts": {
    "build:platform": "./scripts/build-platform-package.sh",
    "build:public": "npm run --workspace smolvm-embedded build:ts",
    "build": "./scripts/build-current-platform.sh",
    "test": "npm run build:platform && npm run --workspace smolvm-embedded test",
    "smoke": "./scripts/smoke-install.sh"
  },
  "workspaces": [
    "smolvm-embedded"
  ],
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0",
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.19.3",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
